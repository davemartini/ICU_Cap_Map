import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({

  mapContainer: {
    height: '93.4vh', width: '100%',
  },
  pointer: {
    cursor: 'pointer',
  },
}));

